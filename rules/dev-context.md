# 재개 컨텍스트

- 2026-09-24 **저지연 후보 구현·커밋 완료**: 입력 상태기는 도착100ms·최소 누름35ms를 유지하고, 해제 후 체류를60ms·제스처 간격을0ms로 낮췄다. 0ms/기한 만료 단계는 다음 감시 tick을 기다리지 않고 같은 worker 메시지에서 진행하며, Move 대기열은 순서를 유지한 채 continuation wake로 처리한다. watchdog·capture 감시는 기존 50ms health timer에만 둔다. `TimedPointerLatencyTests`와 기존 Infra 검증을 포함해 Core65·Infrastructure76 통과, build 경고0/오류0, Release publish 후보 SHA `40B44D0D727115A09952543DE60E62DD62FAD14F6F79A268EF0B56F1B5C479B9`를 만들었다. 정본 `C:\app\Magnifier.App.exe`는 기존 SHA `C7DB1CB5…` 그대로이며 새 후보는 아직 교체·실제 게임 검증하지 않았다. 클릭·더블클릭·드래그 전달 유지와 게임 반응은 `NEEDS_USER_UI_CHECK`다. 성능 변경 커밋은 이 상태를 기준으로 한다.
- 이전 16:20 배포본은 post-Up560ms 단독 비교본: `C:\app\Magnifier.App.exe`, SHA `6BD56E9EA521CC89A7FC8521F33EC252300EC1C00E63C1386F019EDBBB472B44`. 이 버전의 종료 후 검증은161테스트 통과였으며, 이전18DD8FD1 실행본은 `C:\app\Magnifier.App.pre-post-up-dwell-20260922-162001.exe`로 복구 가능하다. 사용자의 게임 결과는5회 중1회 랜덤이었고 PID69992 로그10회 모두 Down 직전 explorer root라 조회값만으로 성공/실패를 판단할 수 없었다.
- 2026-09-22 15:58~16:03 읽기 전용 진단: `relay-stop-61660.jsonl`의 첫59회는 Down 시작59회(조회 root: 게임3347148 11회, explorer/셸48회)이며 게임 수신 결과는 기록하지 않는다. 실제 PawMart 창3347148/PID4444는 같은 원본좌표(3120,1850)에서 ExStyle `0x80000`→`0x80020`로 변하고 조회 대상도 게임→바탕화면으로 변함을 관측했다. 게임 배포에 `LibUniWinC.dll`이 있다. 직접 수신 시점과의 인과는 미확정이다. `scripts/probes/Read-RelayWindows.ps1`은 커서/창 상태만 읽으며 UI를 조작하지 않는다. 사용자 확인: new-alt도 확대 화면 직접 클릭으로 성공. 실제 new-alt 경로는 `OverlayEditorController.ReplicaPanelDirectGesture`→`RuntimePathLab`이며 일반 `RemotePointerOutputAdapter`와 다르다. 탭은 사용자 Up 뒤 독립 클릭(35ms 유지+60ms 회복+500ms 복귀 대기), 6DIP 이상 드래그는 고정 HWND 동기 메시지 경로다. Pinte의 즉시 Down/즉시 복귀와 차이가 있으며 API 이름만 이식했다고 동등하다고 판단하지 않는다. 이 진단에서는 앱 코드·정본 실행 파일을 변경하지 않았다.
- 2026-09-22 Astra 리뷰 후 입력 회귀 복구: 모든 `LLMHF_INJECTED`를 우회하던 실행본 `4A6625D6…C8E26D`를 제거하고, 자기 `InjectionTag`만 제외하는 tagged `SendInput` 이동·버튼 경로로 복원했다. `SetCursorPos` 좌표·250ms 추정 필터와 조건부 passthrough 실험도 철회했다. 훅/마우스 처리는 `WindowsLivePointerRelay.Mouse.cs`로 분리했다. 보조 주입 입력/자기 출력 혼합·3회 연속·버튼 상태 회귀 테스트 10개 추가; 이전 오류를 재현하면 6개 실패, 복원 후 build 경고0/오류0·Core60/Infrastructure41/App55 총156 통과. Astra 최종 리뷰 PASS는 이 회귀 복구 범위다.
- **원래 게임의 간헐 클릭 실패는 미해결**이다. `dwExtraInfo`를 게임이 차단한다는 증거는 없고, 입력 전송/재무장 로그는 게임 수신 증거가 아니다.
- delivery workspace는 `C:/ai/projects/magnifier`의 현재 브랜치다. 이번 소스·테스트·문서 변경만 하나의 로컬 커밋으로 남기며, `notes/transfers/`와 도구 산출물은 스테이징·수정하지 않는다. 푸시는 하지 않는다.
- 2026-09-18 릴리즈: Pinte v0.1.1은 화면 캡처 숨김 기본 꺼짐 설정·한 줄 콤보 목록·접힌 렌즈 `⌕` 이동과 클릭 펼치기 분리를 포함한다. 표준 build 0/0·총 146 테스트 통과, 실제 UI·대상 앱 입력은 `NEEDS_USER_UI_CHECK`다.
- 2026-09-17 구현: 렌즈를 화면 밖으로 옮기거나 그 방향으로 키워도 작업 영역으로 되돌리지 않는다. 8방향 외곽 리사이즈는 하단 모서리 48 DIP·변 20 DIP이고, 이동 손잡이와 겹치던 상단 모서리는 24 DIP로 줄여 `SizeAll` hover를 우선한다.
- `렌즈 숨김`은 복귀가 아니다. 클릭 지점의 캡처 제외 `⌕` 오버레이로 접고, 그 아이콘을 누르면 기존 렌즈 위치·크기·배율을 유지한 채 새 프레임과 입력을 다시 연다. 아이콘 표시 또는 펼치기 실패는 렌즈/입력을 안전하게 중지하거나 기존 렌즈 표시를 유지한다.
- 2026-09-22 수정·배포: 접힌 렌즈 `⌕`는 표시·이동할 때 비활성 최상위 Z-order로 명시 배치한다. 실행 중인 앱을 다시 실행하면 세션 한정 활성화 신호가 기존 인스턴스에 전달되어 release-first 복귀 경로로 진입 창을 전면에 복원한다. Windows x64 단일 파일 publish SHA-256 `073F599C…E2B881`은 `C:\app\Magnifier.App.exe`에 교체됐고 PID 72100이 응답 상태로 시작했다. 이전 0.1.0은 `C:\app\Magnifier.App.pre-collapse-recovery-20260922-0132.exe`로 복구 가능하다. 실제 일반·최상위 창과의 Z-order 및 두 번째 실행 복귀는 `NEEDS_USER_UI_CHECK`다.
- 좁은 영역 지정 도구막대는 이동 설명을 숨기고 축약 버튼을 사용해 `이 영역 확대`와 복귀가 잘리지 않게 했다. 일반·컴팩트 렌즈 모두 숨김 제어를 제공한다.
- 배율 `−/+`는 0.1× 단위이며, 일반의 세밀 입력과 컴팩트의 중앙 숫자 입력 모두 Enter 또는 포커스 이탈 때 같은 정규화·입력 보류 경로로 적용한다.
- 자동 검증: `dotnet build Magnifier.slnx --nologo` 경고 0/오류 0, `dotnet test Magnifier.slnx --nologo` Core 60·Infrastructure 31·App 52, 총 143개 통과. 실제 UI·대상 앱 입력은 `NEEDS_USER_UI_CHECK`이며 [수동 시나리오](../doc/compact-lens-validation.md)의 화면 밖 배치·리사이즈·이동 hover·접기/펼치기·직접 입력·좁은 선택 폭을 확인한다.
- 핵심 제약: UI는 Win32 캡처·입력 주입을 직접 호출하지 않는다. 창·영역·배율·리사이즈·숨김/펼치기 전후에는 release와 새 프레임을 우선하고, 명시적 중지·복귀·닫기·입력 실패는 자동 재개하지 않는다.
- 현재 공개 기준은 [Pinte v0.1.1](https://github.com/ai-blink/pinte/releases/tag/v0.1.1)이다. 개발용 표준 출력은 `src/Magnifier.App/bin/Debug/net9.0-windows/Magnifier.App.exe`, 공개 배포본은 Windows x64 자체 포함 ZIP이다.
- 별도 미해결 범위: 실제 상대 마우스의 이중 포인터, 경계/오류별 release·복귀, 혼합 DPI·음수 좌표·다중 모니터, 브라우저/Blender/Windows 앱 호환. 기존 훅 워커 복구(D-030) 장기 확인도 `NEEDS_USER_UI_CHECK`다.
